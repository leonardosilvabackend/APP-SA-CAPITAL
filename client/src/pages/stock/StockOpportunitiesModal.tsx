import { Sparkles, X } from "lucide-react";
import AdministratorLogo from "../../components/AdministratorLogo";
import { dismissBackdrop } from "../../lib/dismissBackdrop";
import { stockCurrency, type Opportunity } from "./contracts";
export default function StockOpportunitiesModal({ items, onChoose, onClose }: { items: Opportunity[]; onChoose: (ids: string[]) => void; onClose: () => void }) { return <div className="modal-backdrop" {...dismissBackdrop(onClose)}><section className="stock-modal opportunity-modal"><div className="modal-heading"><div><span className="eyebrow">DESTAQUES</span><h2>Oportunidades do dia</h2></div><button className="icon-button" onClick={onClose}><X size={19} /></button></div>{items.map(item => <article className="opportunity-card" key={item.id}><div className="opportunity-title"><AdministratorLogo name={item.quotas[0]?.administrator ?? "SA"} /><div><strong>{item.quotas[0]?.administrator ?? "Oportunidade"}</strong><span>Cotas {item.quotas.map(quota => quota.code).join(", ")}</span></div></div><div className="summary-highlight"><span>Crédito total</span><strong>{stockCurrency(item.summary.creditTotal)}</strong></div><p className="opportunity-reason"><Sparkles size={17} /> {item.reason}</p><button className="primary-button button-reset" onClick={() => onChoose(item.quotas.map(quota => quota.id))}>Ver cotação</button></article>)}</section></div>; }
