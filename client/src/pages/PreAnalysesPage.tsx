import { dismissBackdrop } from "../lib/dismissBackdrop";
import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import { ExternalLink, FileCheck2, Plus, Trash2, X } from "lucide-react";
import { useState, type FormEvent } from "react";
import { toast } from "sonner";
import type { AuthenticatedUser } from "@shared/contracts";
import { defaultIncomeDocuments, pfIncomeTypes, pjIncomeTypes } from "@shared/business";
void fetch("/api/settings", { credentials: "same-origin" }).then(response => response.ok ? response.json() : null).then(data => { if (data?.settings?.incomeDocuments) { for (const key of Object.keys(defaultIncomeDocuments)) delete defaultIncomeDocuments[key]; Object.assign(defaultIncomeDocuments, data.settings.incomeDocuments); } }).catch(() => undefined);
type Status="received"|"pending"|"approved"|"rejected"|"documents_requested";
type Document={id:string;documentType:string;fileName:string;size:number};
type Item={id:string;partnerName:string;customerType:"PF"|"PJ";customerName:string;document:string;incomeType:string;status:Status;observations:string|null;createdAt:string;documents:Document[]};
const labels:Record<Status,string>={received:"Recebida",pending:"Em análise",approved:"Aprovada",rejected:"Reprovada",documents_requested:"Solicitar documentos"};
async function api(path="",options?:RequestInit){const response=await fetch(`/api/pre-analyses${path}`,{credentials:"same-origin",...options,headers:options?.body&&!(options.body instanceof Blob)?{"Content-Type":"application/json",...options.headers}:options?.headers});const data=await response.json().catch(()=>({}));if(!response.ok)throw new Error(data.error??"Não foi possível concluir");return data;}
function formatDocument(value:string,type:"PF"|"PJ"){return type==="PF"?value.replace(/(\d{3})(\d{3})(\d{3})(\d{2})/,"$1.$2.$3-$4"):value.replace(/(\d{2})(\d{3})(\d{3})(\d{4})(\d{2})/,"$1.$2.$3/$4-$5");}
function CreateModal({onClose}:{onClose:()=>void}){const qc=useQueryClient();const [type,setType]=useState<"PF"|"PJ">("PF");const [name,setName]=useState("");const [document,setDocument]=useState("");const [income,setIncome]=useState(pfIncomeTypes[0]);const [consent,setConsent]=useState(false);const [files,setFiles]=useState<Record<string,File>>({});const [sending,setSending]=useState(false);const docs=defaultIncomeDocuments[income]??[];async function submit(event:FormEvent){event.preventDefault();if(!consent)return toast.error("O consentimento é obrigatório");if(docs.some(doc=>!files[doc]))return toast.error("Anexe todos os documentos solicitados");setSending(true);try{const created=await api("",{method:"POST",body:JSON.stringify({customerType:type,customerName:name,document,incomeType:income,consent:true})});for(const doc of docs){const file=files[doc];await api(`/${created.item.id}/documents`,{method:"POST",body:file,headers:{"Content-Type":file.type,"X-File-Name":encodeURIComponent(file.name),"X-Document-Type":encodeURIComponent(doc)}});}await qc.invalidateQueries({queryKey:["pre-analyses"]});toast.success("Pré-análise enviada com sucesso. O retorno será enviado para o e-mail cadastrado.");onClose();}catch(error){toast.error((error as Error).message);}finally{setSending(false);}}return <div className="modal-backdrop pre-analysis-backdrop" {...dismissBackdrop(onClose)}><form className="pre-analysis-modal" onSubmit={submit}><div className="modal-heading"><div><span className="eyebrow">NOVA PRÉ-ANÁLISE</span><h2>Dados e documentos</h2></div><button type="button" className="icon-button" onClick={onClose}><X size={19}/></button></div><div className="pre-analysis-form"><label>Tipo<select value={type} onChange={e=>{const value=e.target.value as "PF"|"PJ";setType(value);setIncome((value==="PF"?pfIncomeTypes:pjIncomeTypes)[0]);setFiles({});}}><option value="PF">Pessoa física</option><option value="PJ">Pessoa jurídica</option></select></label><label>Nome / Razão social<input value={name} onChange={e=>setName(e.target.value)} required minLength={3}/></label><label>{type==="PF"?"CPF":"CNPJ"}<input value={document} onChange={e=>setDocument(e.target.value.replace(/\D/g,"").slice(0,type==="PF"?11:14))} required/></label><label>Tipo de renda<select value={income} onChange={e=>{setIncome(e.target.value);setFiles({});}}>{(type==="PF"?pfIncomeTypes:pjIncomeTypes).map(value=><option key={value}>{value}</option>)}</select></label></div><div className="required-documents"><h3>Documentos obrigatórios</h3>{docs.map(doc=><label key={doc}><span>{doc}</span><input type="file" accept=".pdf,.jpg,.jpeg,.png" required onChange={e=>{const file=e.target.files?.[0];if(file)setFiles(current=>({...current,[doc]:file}));}}/></label>)}</div><label className="consent-check"><input type="checkbox" checked={consent} onChange={e=>setConsent(e.target.checked)}/> Declaro que tenho autorização do titular dos dados para enviar estes documentos à SA CAPITAL para fins de pré-análise de crédito.</label><p className="privacy-box">Documentos guardados em ambiente privado, acessível só à equipe.</p><div className="form-actions"><button type="button" className="secondary-button" onClick={onClose}>Cancelar</button><button className="primary-button button-reset" disabled={sending}>{sending?"Enviando…":"Enviar pré-análise"}</button></div></form></div>}
export default function PreAnalysesPage({user}:{user:AuthenticatedUser}){const qc=useQueryClient();const [creating,setCreating]=useState(false);const [selected,setSelected]=useState<Item|null>(null);const list=useQuery<{items:Item[]}>({queryKey:["pre-analyses"],queryFn:()=>api()});const update=useMutation({mutationFn:({id,status,observations}:{id:string;status:Status;observations:string})=>api(`/${id}`,{method:"PATCH",body:JSON.stringify({status,observations})}),onSuccess:()=>{void qc.invalidateQueries({queryKey:["pre-analyses"]});toast.success("Pré-análise atualizada");setSelected(null);}});const remove=useMutation({mutationFn:(id:string)=>api(`/${id}`,{method:"DELETE"}),onSuccess:()=>{void qc.invalidateQueries({queryKey:["pre-analyses"]});void qc.invalidateQueries({queryKey:["dashboard-metrics"]});toast.success("Pré-análise excluída");setSelected(null);},onError:(error:Error)=>toast.error(error.message)});const canAnalyze=["admin","administrative"].includes(user.role);async function openDocument(item:Item,doc:Document){const data=await api(`/${item.id}/documents/${doc.id}`);window.open(data.url,"_blank","noopener,noreferrer");}return <section className="pre-analyses-page"><div className="page-heading-row"><div><span className="eyebrow">ANÁLISE CADASTRAL</span><h1>Pré-análises</h1><p>{list.data?.items.length??0} registros encontrados.</p></div>{user.role!=="administrative"&&<button className="primary-button button-reset" onClick={()=>setCreating(true)}><Plus size={18}/> Nova pré-análise</button>}</div><div className="pre-analysis-list panel">{list.isLoading&&<div className="table-message">Carregando…</div>}{list.error&&<div className="auth-error">{list.error.message}</div>}{list.data?.items.map(item=><button className="pre-analysis-row" key={item.id} onClick={()=>setSelected(item)}><span className="pre-analysis-icon"><FileCheck2 size={20}/></span><div><strong>{item.customerName}</strong><small>{item.customerType} • {formatDocument(item.document,item.customerType)} • {item.incomeType}</small></div><div><small>Usuário</small><strong>{item.partnerName}</strong></div><div><small>Data</small><strong>{new Date(item.createdAt).toLocaleString("pt-BR")}</strong></div><span className={`analysis-status ${item.status}`}>{labels[item.status]}</span></button>)}</div>{creating&&<CreateModal onClose={()=>setCreating(false)}/>} {selected&&<div className="modal-backdrop pre-analysis-backdrop" {...dismissBackdrop(() => setSelected(null))}><div className="pre-analysis-modal"><div className="modal-heading"><div><span className="eyebrow">PRÉ-ANÁLISE</span><h2>{selected.customerName}</h2></div><button className="icon-button" onClick={()=>setSelected(null)}><X size={19}/></button></div><p>{selected.incomeType} • {formatDocument(selected.document,selected.customerType)}</p><div className="document-list">{selected.documents.map(doc=><button className="secondary-button" key={doc.id} onClick={()=>void openDocument(selected,doc)}><ExternalLink size={15}/> {doc.documentType}: {doc.fileName}</button>)}</div>{canAnalyze?<form onSubmit={e=>{e.preventDefault();const form=new FormData(e.currentTarget);update.mutate({id:selected.id,status:String(form.get("status")) as Status,observations:String(form.get("observations"))});}}><label>Status<select name="status" defaultValue={selected.status}>{Object.entries(labels).map(([value,label])=><option key={value} value={value}>{label}</option>)}</select></label><label>Observações<textarea name="observations" defaultValue={selected.observations??""}/></label><div className="form-actions"><button type="button" className="danger-button" disabled={remove.isPending||update.isPending} onClick={()=>{if(window.confirm(`Excluir a pré-análise de ${selected.customerName} e todos os documentos anexados? Esta ação não pode ser desfeita.`))remove.mutate(selected.id);}}><Trash2 size={16}/>{remove.isPending?"Excluindo…":"Excluir pré-análise"}</button><button className="primary-button button-reset" disabled={remove.isPending||update.isPending}>Salvar análise</button></div></form>:selected.observations&&<p>{selected.observations}</p>}</div></div>}</section>}
